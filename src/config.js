export const BASE_URL = 'http://54.180.131.239:8000';
const API = {
  signin: `${BASE_URL}/users/signin`,
  stays: `${BASE_URL}/stays`,
  hosting: `${BASE_URL}/stays/hosting`,
};

export default API;
